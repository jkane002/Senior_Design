using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class appearReady : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        appear.ready = true;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
